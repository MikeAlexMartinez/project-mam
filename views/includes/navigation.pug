section(id="navigation" class=`navigation ${location === 'home' ? 'home' : ''}`)
  // Hamburger menu - hidden on widescreen
  div(id="menu-icon")
    span
    span
    span
  div.items.top
    div#home-nav.item
      a(href="/")
        h3(class=`itemTitle ${location === 'home' ? 'red' : ''}` ) Project MaM
        div(id="smallLogo" class=`imgContainer ${location === 'home' ? 'red' : ''}`)
    div#projects-nav.item
      a(href="/projects")
        h3(class=`itemTitle ${location === 'projects' ? 'red' : '' }`) Projects
        div(class=`imgContainer ${location === 'projects' ? 'red' : '' }`)
          i.ion-ios-folder
    div#blogs-nav.item
      a(href="/blog")
        h3(class=`itemTitle ${location === 'blog' ? 'red' : '' }`) Blog
        div(class=`imgContainer ${location === 'blog' ? 'red' : '' }`)
          i.ion-ios-paper
  div.items.bottom
    div#contact-nav.item
      a(href="/#contact-name" class=`${location === 'home' ? 'red' : '' }`)
        h3.itemTitle Contact
        div.imgContainer
          i.ion-ios-megaphone
    div#bug-report.item
      a(href="#")
        h3.itemTitle Report Bug
        div.imgContainer
          i.ion-ios-bug

section.bug-reporter
  div.br-container
    h1 Call the #[span Exterminator!]
    p Please let me know if you experience any issues with my site 
    div.bugform.contact
      form(class="form-group" action="" id="bugReporter")
        input(id="bug-source" class="hidden" type="text" value="project-mam" name="source" hidden)
        div.mainInputs
          div.formSide.left
            input(id="bug-name" class="entry" type="text" placeholder="Name" name="name" required)
            input(id="bug-email" class="entry" type="email" placeholder="Email" name="email" required)
          div.formSide.right
            textarea(id="bug-message" class="entry" type="text" placeholder="Bug description" name="message" rows="4" required)
        div.clicky
          button(type="submit" class="btn") Send
          div#bugToast.toast
      
    